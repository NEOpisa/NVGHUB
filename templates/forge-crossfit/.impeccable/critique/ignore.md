# Critique ignore list

Findings matching these are dropped silently on future `/impeccable critique` runs.

- `R$ 0,00` / zero pricing on plan cards — intentional placeholder; this is a sellable template, not a live site. Do not flag as a bug or trust issue.
- `[Nome da Academia]` and similar bracketed placeholders — intentional template fill-ins.
